<p>You are here 
<?
foreach($this->breadcrumbs as $crumb)
{
    $title = $crumb[0];
    $url = $crumb[1];
    echo " &gt; ";
    echo "<a href='".fullbase()."/$url'>$title</a>";
}
echo " &gt; ".$this->page_title."</p>";

